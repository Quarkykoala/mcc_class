{
    "name": "mcc_class",
    "version": "1.0.0",
    "private": true,
    "workspaces": [
        "apps/*"
    ],
    "scripts": {
        "dev": "concurrently \"npm run dev -w @mcc/api\" \"npm run dev -w web\"",
        "test": "echo \"Error: no test specified\" && exit 1"
    },
    "keywords": [],
    "author": "",
    "license": "ISC",
    "description": "",
    "devDependencies": {
        "@types/supertest": "^6.0.3",
        "concurrently": "^9.2.1",
        "supertest": "^7.2.2"
    }
}